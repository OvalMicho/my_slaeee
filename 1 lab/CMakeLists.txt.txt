cmake_minimum_required(VERSION 3.20)			#Используемая версия 
project(ThreeD)						#Имя проекта

find_package(GTest REQUIRED)

enable_testing()

add_executable(ThreeD alltest.cpp srcfunc.hpp src.cpp)	#Создание исполняемого файла
target_link_libraries(ThreeD GTest::gtest_main)		#Определение зависимости файла от библиотеки

add_test(ALLTest ThreeD)